package com.efkrdnz.magical.client.hud;

/**
 * A screen that dims the world and carries no plate of its own, so the modded HUD would read
 * straight through it. While one is open every layer in {@link HudLayers} stands down, the way the
 * sigil does for the rule selector, and comes back when it closes.
 */
public interface HudQuiet {
}
