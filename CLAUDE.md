# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

"The Iceberg of Magic" — a NeoForge mod for Minecraft **1.21.4** (NeoForge 21.4.157, Java 21). Mod id: `magical`, base package `com.efkrdnz.magical`. Design intent in `Iceberg_of_Magic_Design_Document.md` (tiered magic: Arcane → Elemental → hidden Authority endgame) and `SKILL_CREATION_NOTES.md` (visual contract: no reused vanilla projectiles, custom renderers + shaders).

## Commands

```powershell
.\gradlew build                      # compile + assemble mod jar
.\gradlew runClient                  # dev Minecraft client
.\gradlew runClient -PquickPlay="New World" -PautoScreenshot=200   # join a world at launch; screenshot 200 ticks in
.\gradlew runClient -PquickPlay="New World" -PautoScreenshot=300 -PautoCommands="magical unlockall;magical hud equip 1 gabriel" -PautoHold=cast_slot_2   # ... after commands at tick 40, holding X for the last 60 ticks
.\gradlew runClient -PquickPlay="New World" -PautoCommands="magical hud rule gravity increase_gravity;100:magical hud rule velocity stop" -PautoScreenshot=52,112 -PautoExit   # commands at ticks 40 and 100, a capture at each, then the game closes itself
.\gradlew runServer                  # dev dedicated server (--nogui)
.\gradlew runData                    # run data generators → src/generated/resources/
.\gradlew runGameTestServer          # run registered gametests headlessly
.\gradlew test                       # JUnit 5 unit tests
.\gradlew test --tests "com.efkrdnz.magical.SomeTest"  # single test class
```

- On macOS/Linux use `./gradlew`; Gradle 9.2 needs a JDK ≤ 25 as `JAVA_HOME` (the Java 21 toolchain is auto-provisioned).
- `src/main/templates/META-INF/neoforge.mods.toml` is a template; `${mod_id}` etc. are expanded from `gradle.properties`. Edit template + `gradle.properties`, never generated `mods.toml`.
- `src/generated/resources/` is a resources source dir alongside `src/main/resources/`. Datagen output.

## Architecture

### Entry point & registration

`MagicalMod.java` (`@Mod("magical")`) wires all `DeferredRegister` holders from `registry/` onto the mod bus. Each registry class follows the same pattern:
- `registry/MagicalBlocks.java` — `DeferredRegister.Blocks`
- `registry/MagicalItems.java` — `DeferredRegister.Items` (all `BlockItem`s)
- `registry/MagicalEntities.java` — ~40 entities, almost all `MobCategory.MISC`, invisible gameplay entities
- `registry/MagicalMenus.java`, `registry/MagicalAttachments.java`, `registry/MagicalCommands.java`, `registry/MagicalChunkTickets.java`, `registry/MagicalCreativeTabs.java`, `registry/MagicalBlockEntities.java`

Game-bus handlers are `@EventBusSubscriber(modid = MODID)` annotated classes — never manual `NeoForge.EVENT_BUS.register`.

### Three magic systems

1. **Arcane (Tier 1, composable spell-crafting)** — `arcane/` package. Runes + shapes + modifiers defined in `ArcaneContent`, resolved by `ArcaneSpellResolver`, cast via `ArcaneCastingService`. Has its own `AttachmentType` (`ARCANE_DATA`), own sync payload (`ArcanePlayerDataPayload`), own client mirror (`ClientArcaneState`). Separate from `magic/`.

2. **Elemental skill-based (Tiers 0-5)** — `magic/` package. ~80+ skills defined as `MagicSkillDefinition` records in `MagicContent` (static `register(...)` calls with base stats). Skills classified into sets controlling random unlocks/GUI: `SUB_SKILLS`, `AUTHORITY_SKILLS`, `CLASS_REWARD_SKILLS`, `CREATED_SKILLS`. Negative `tier` values mean hidden/authority content, excluded from random rewards.

3. **Authority (hidden endgame)** — `magic/AuthorityContent.java` defines endgame unlocks (Space, Soul, Chronos). Gated behind the Authority dimension progression. Skills like `CREATE_SUBSPACE`, `MANIPULATE_SPACE`, `POCKET_DIMENSION`, `SOUL_VOW`.

### Cast flow (keybind → render)

1. **Client tick** — `MagicalClientEvents.Hud.onClientTick` polls `MagicalKeyMappings` (Z/X/C/V = 4 loadout slots, B = loadout switcher / counter answer, K = codex, R = barrier refill). Hold/charge skills (Barrage, Subspace, Aegis, Black Flames, etc.) are intercepted by per-skill input handlers (`MagicBarrageInput`, `SpaceAuthorityInput`, `SovereignAegisInput`, etc.) before normal cast. Normal press sends a `CastLoadoutSlotPayload`.

2. **Server dispatch** — `MagicalNetwork` routes payloads. `MagicCastingService.castResolved` checks cooldown/unlock/mana, then routes via an **`if`-ladder** (special skills first: Vault of Avarice, Subspace, Space Walker, Aegis, etc.), then a `switch` on `MagicSkillType` (`PROJECTILE`/`BURST`/`BARRIER`). That `if`-ladder is the real routing table — every skill with custom behavior has an explicit ID check.

3. **Entity + renderer** — Service spawns a custom entity from `MagicalEntities`. The renderer (bound in `MagicalClientEvents.registerRenderers`) draws it using custom quads/beams/rings via `MagicalRenderTypes`.

### Player data & sync

- Two `AttachmentType`s, both `.copyOnDeath()`, registered in `MagicalAttachments`: `MAGIC_STATE` (`PlayerMagicState`) and `ARCANE_DATA` (`ArcanePlayerData`).
- Sync is copy-based and wholesale: `state.sync(player)` → `PlayerMagicStatePayload` → client; `MagicalNetwork.syncArcaneData(player, data)` → `ArcanePlayerDataPayload`.
- Static client singletons (`ClientMagicState`, `ClientArcaneState`) replace their contents on payload receipt.
- Side safety: `MagicalNetwork.handleClientPayload` uses `FMLEnvironment.dist.isClient()` + reflection into `client/ClientPayloadHandlers` — server never classloads client code.

### Network

All payload records in `network/` (24 payloads). `MagicalNetwork` registers them in `registerPayloads` using `event.registrar("1")`. Play-to-client: state sync, first-person effects, counter prompts. Play-to-server: cast requests, hold/charge state, wheel casts, class actions, space rule applications.

### Classes

`classes/MagicalClasses.java` — 16 classes (Blacksmith → Divinesmith, Warrior → Berserker/Warden, etc.) as `MagicalClassDefinition` records with parent class, XP cost, granted skill IDs. Tree-based progression tracked in `MagicalClassProgress`. `PlayerMagicState.evolveClass` validates prerequisites and grants skills.

### Passives & curses

`magic/MagicPassiveContent.java` — 8 normal passives (Mana Skin, Heat/Poison/Magic Resistance, Mana Flight, etc.) + 7 sin passives + 7 curses + 1 mana leak curse. Normal passives have a disable checkbox. Curses have no checkbox — require a Dispel action with conditions. Passives appear in a separate tab in the Magic Codex.

### Blood school

Layer -1, seven actives paid in blood (`BloodService.pay`: the Vessel first, then health at 8 per heart; `BloodService.cost(stats)` is the `BloodPrices` base scaled by `MagicSkillResolvedStats.costScale()`, the same factor mana is billed at, so Thrift and Bite move the price; a zero mana base resolves to zero). Blood lives in three places: the Vessel, pools on the ground, the body. Pools are one `entity/BloodHarvestEntity` each (never saved) of three kinds from `BloodHarvestRules`: a **harvest** (a kill or an Open Vein bite) lifts on its own within pull range (12; Bloodscent 20), streams into the chest and credits the Vessel on landing; a **battery** (Blood Rite, a Spear impact) waits to be spent by Coagulate, the Spear or a Vein Walk landing; a **trace** (where a walk began) is worth nothing. Each pool owns its life (Clotting doubles it); a fed pool remembers its source and feed window and neither lifts while fed nor for a grace after. The skills, one file each in `magic/skill/blood/`: Blood Manipulation (the drawn field), Vein Walk (marked enemy in view, else the far end of a live field, else the furthest pool; leaves a trace and a vein stream), Open Vein (bites every 10 ticks, sheds into a pool at the feet, a new one when the victim moves 2 blocks), Crimson Spear (a field anchored on its own entity, gathers at the hand then flies, shatters into a battery), Coagulate (drinks pools in Draw plus the Vessel, sets a ring whose synced `integrity` follows the barrier it holds), Blood Rite (hold: a heart per pulse into a battery ahead). Client: `BloodVoxels` paints fields (owner- or entity-anchored, integrity erosion), `BloodHarvestRenderer` draws pools, the drops out of a bleeding body (`BloodBleedMotion`, the harvest flight reversed), streams and the vein; styles in `VoxelStyle` (STRIKE, HARVEST, SPEAR, SHELL); painter ids in `BloodPainters`. Captures: `magical-debug skill magical:<id>` casts by id, `/magical blood harvest [amount]` spills a harvest pool six blocks ahead, `magical hud vessel <n>` fills the Vessel, `-PautoCamera=third_back` looks at the player from behind (the shell, the vein, the pour). The equip slots are zero-based and the key mappings one-based (`magical hud equip 1 blood_rite` pairs with `-PautoHold=cast_slot_2`); after `magical reset` choose the race (`magical hud race human`) and keep screenshot ticks off multiples of ten, or the onboarding screen is in the shot. First person, a golem bled then speared: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;magical reset;magical hud race human;magical unlockall;magical hud vessel 100;time set day;tp @s ~ ~ ~ 0 0;summon iron_golem ~ ~ ~5 {NoAI:1b};120:magical-debug skill magical:open_vein;220:magical-debug skill magical:crimson_spear" -PautoScreenshot=133,155,195,225,236,262 -PautoExit`. Third person, a rite held, the walk onto its battery, then the shell: the same launch with `-PautoCommands="...;magical hud vessel 30;tp @s ~ ~ ~ 0 0;magical hud equip 1 blood_rite;200:magical-debug skill magical:vein_walk;250:magical-debug skill magical:coagulate" -PautoHold=cast_slot_2 -PautoScreenshot=185,189,203,207,222,259,268,300 -PautoCamera=third_back -PautoExit`. Designs: `docs/superpowers/specs/2026-09-13-blood-harvest-design.md`, `docs/superpowers/specs/2026-09-14-blood-kit-design.md`.

**Blood Sacrifice** is the seventh, and the only one that will not take a heart instead: it wants a full Vessel (`BloodSacrificeService.RITUAL_COST` = 100) and refuses anything less. Health paid anywhere in the school is now true damage: `magical:blood_price` (`magic/blood/BloodDamageTypes`) sits in four bypass tags and `MagicGameplayEvents.onIncomingDamage` returns early for it, so neither armour, resistance, a passive nor the barrier takes anything off the bill, and a heart is worth `BloodService.COST_PER_HEALTH` = 25, ten hearts the whole 500. Pressing the skill opens a pact screen (`OpenBloodSacrificePayload` -> `client/screen/sacrifice/BloodSacrificeScreen`, geometry in `BloodSacrificeLayout` pinned by its test): boons on the left spend points, prices in the middle pay for them, the pact on the right seals. The budget is `SacrificeBudget.boonBudget` (four, plus a point per point of Pact size, plus one on credit for a Hellbroker); `SacrificeCatalogue` holds sixteen boons and sixteen prices with their costs; the effects live in `magic/passive/SacrificeBoons` and `SacrificeCurses` behind one `SacrificePassives` handler. Sealing sends `BloodSacrificeSealPayload` (both lists capped on the wire) to `BloodSacrificeService.seal`, which re-checks every rule, empties the Vessel, grants both halves as temporary passives on `PlayerMagicState.ritualTicks` and starts the cooldown. A price runs `PRICE_DURATION_FACTOR` (1.5x) longer than the boon it paid for; The Unknown resolves on its weight table at the seal; Blood Debt has no clock at all and pays in 25 corruption. Neither half can be switched off - `togglePassive` refuses anything carrying a ritual clock. The codex tab has two columns and the pact has two halves, so they line up: `CodexPassiveRows.rows` pins the boons under **Temporary Buff** above the permanent passives and `curseRows` pins the prices under **Temporary Curse** above the curses, which pick up a **Lasting** heading only when a pact is running. Every pact row carries an m:ss countdown in place of the Enabled line, a price is drawn on the curse ground with the curse tooltip, and neither half has a checkbox or a Dispel button - the clock is the only way out. Hellbroker (`SacrificeBudget.amplification`) buys that extra point and makes every price after the first harsher, `1 + 0.5*(n-1)` capped at 2.25. Captures: `/magical sacrifice` opens the screen without walking a Vessel up first, and the onboarding must be satisfied first or the class chooser reopens over it every 20 ticks: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;magical class unlock mystic;magical unlockall;magical hud vessel 100;time set day;141:magical sacrifice;200:magical codex" -PautoClick="158:186,84;166:186,150;174:338,84;182:338,172;198:471,278;214:339,45" -PautoScreenshot=190,222 -PautoExit`. A fake player in `runGameTestServer` never receives `PlayerTickEvent`, so `BloodSacrificeGameTests` drives `tickServer` itself. Design: `docs/superpowers/specs/2026-09-15-blood-sacrifice-design.md`.


### Eldritch school

Layer -5, six calls paid in mana and in **Notice** (`PlayerMagicState.notice`, 0..100: `EldritchService.notice` adds the `EldritchPrices` base scaled by `costScale`, `EldritchPassives.slowTick` cools it a point per slow tick, `potency` scales every construct 1..1.5 by it; Watched at 50 turns nearby hostiles on the caster, Noticed at 100 has the deep grasp the caster every 200 ticks). Three creatures, one entity: `entity/fx/EldritchConstructEntity` (a `SpellEffectEntity` with a model name, an anchor - GROUND, OWNER, TARGET - and a scale; its radius is the skill's reach) drawn by `client/renderer/eldritch/EldritchConstructRenderer` (the profile FX through `ProfileRendererShell` with the FX radius zeroed so silhouettes keep their written sizes, then the model). Models are the Bedrock geometry Blockbench exports: `assets/magical/models/entity/eldritch/<name>.geo.json` + `textures/entity/eldritch/<name>.png` (+ `<name>_glow.png`, drawn full bright over the body on `RenderType.eyes`, which blends rather than adds, so it is transparent wherever the creature does not glow; `EldritchGlowTexturesTest` holds the placeholders to that), Box UV only, read by `client/model/geo/GeoModelParser` (pure), baked by `GeoModelBaker` (x as written, y negated, z as written, degrees with the same sign; a rotated cube becomes its own part), held by `client/model/eldritch/EldritchModels` (reload listener: F3+T reloads an export). Motion is procedural in `EldritchPose` (chain CCD toward the target, growth from the base, dissolve, gaze ease, jaws; `EldritchPostureTest` pins the jaw and gaze directions in the y-down frame against the placeholders): the tentacle bones are `seg0..seg5` under `root`, the eye is `body` + `pupil` (+ optional `lid_upper`/`lid_lower`), the maw is `jaw_upper` + `jaw_lower` hinged at the back. Skills in `magic/skill/eldritch/`, aimed with the handler's default tolerance (`AimResolver` skips entities at zero): Grasp of the Deep (a ground tentacle erupting `beside()` the victim toward the caster, roots and crushes; also the Noticed rung's grasp and Call's children), Unblinking Eye (a floating eye hung just off what it looks at: reveals, +25% spell damage on what it sees, stings), Hungering Maw (jaws open under what the caster looks at, wait, snap: `syncedData` `windup`/`snap`), Tendril Lash (owner-anchored whip from the right shoulder through a forward arc, `syncedData` `whip`), Skin of the Deep (`extra` wards on the back absorb hits via `EldritchPassives.incomingDamage`, hidden from the wearer in first person), Call of the Deep (held: an eye above that keeps its height, a grasp under a random hostile per pulse, mana and Notice per pulse). Placeholders: `python scripts/eldritch-placeholders.py`. Game tests (`EldritchGameTests`): husks as victims (zombies burn in the daylight of the test world), one batch per test with leftover fake players removed, the fake player set client-loaded (invulnerable otherwise) and stood on the floor (no client moves it, so it never falls). Captures: `magical hud notice <n>` sets the gauge; unlock only the six after `magical class unlock mystic` (`magical unlockall` fills the three HUD captions and hides the notice line) and clear the beach first (`gamerule doMobLoot false;gamerule doMobSpawning false;kill @e[type=!player]`, or a cave zombie the Watched rung turned on the caster burns in the shot and drops its flesh). First person, a golem grasped, watched, bitten and lashed: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;gamerule doMobLoot false;gamerule doMobSpawning false;kill @e[type=!player];magical reset;magical hud race human;magical class unlock mystic;magical unlock grasp_of_the_deep;magical unlock unblinking_eye;magical unlock hungering_maw;magical unlock tendril_lash;magical unlock skin_of_the_deep;magical unlock call_of_the_deep;magical hud notice 60;time set day;tp @s ~ ~ ~ 0 0;summon iron_golem ~ ~ ~5 {NoAI:1b};120:magical-debug skill magical:grasp_of_the_deep;200:magical-debug skill magical:unblinking_eye;270:magical-debug skill magical:hungering_maw;340:magical-debug skill magical:tendril_lash" -PautoScreenshot=133,151,255,291,297,345,351 -PautoExit`; third person, the call held then the skin: the same launch with `-PautoCommands="...;summon husk ~ ~ ~4 {NoAI:1b};summon husk ~3 ~ ~3 {NoAI:1b};magical hud equip 1 call_of_the_deep;150:magical-debug skill magical:skin_of_the_deep" -PautoHold=cast_slot_2 -PautoScreenshot=157,171,245,261,275 -PautoCamera=third_back -PautoExit`. Design: `docs/superpowers/specs/2026-09-14-eldritch-kit-design.md`.

### Space authority laws

Manipulate Space writes one law per category onto the caster's `SpaceSubspaceEntity`, whose tick runs all twelve over everything inside the radius. **A law has two halves and they run on different machines** (`magic/SpaceLawPass`): the server carries the *consequences* — damage, effects, air, the flight Control Gravity hands out, the body a boundary moves — and each *client* carries the *movement*, against its own player and nobody else's (`client/SpaceLawClient`, on `PlayerTickEvent.Pre`, so a law sets the velocity the player's input then works against). The server never writes a player's velocity because it cannot deliver one: vanilla sends the `hasImpulse` motion packet over `broadcast`, and `ChunkMap.TrackedEntity.updatePlayer` never puts a player in its own audience; the one channel that would reach them, `hurtMarked`, replaces the client's velocity with the server's copy, and nothing in `handleMovePlayer` ever writes a real one. So a law aimed at Everything or Players used to move mobs and do nothing at all to a player — every law except Control Gravity, which works only because abilities have their own packet. Adding a law means threading `SpaceLawPass` through it and putting the `setDeltaMovement` behind `pass.moves()`; `SpaceLawGameTests` fails the moment the server starts pushing players again. Captures: `/magical subspace create [radius] [follow]` raises a domain with none of the skill's gates and `/magical subspace rule <category> <operation> [target]` writes a law onto it (defaulting to `everything`), so one launch drives the real rule loop: `.\gradlew runClient -PquickPlay="New World" -PautoCommands="magical subspace create 8 true;60:magical subspace rule gravity remove_gravity everything" -PautoScreenshot=70,100 -PautoExit`. `/magical hud rule` is a different thing — only the flash, no subspace.

**The boundary** (`client/renderer/SpaceSubspaceRenderer`, geometry in `client/renderer/space/SubspaceVault`, optics in `SubspaceOptics`) is a **vault**, and the whole of it is a reaction to where the camera stands. A domain is a sphere pinned to its caster body centre (`DomainEntity` sets `owner.getY() + bbHeight*0.5`) while the camera is at their eyes, so the eye sits **0.72 blocks above the centre and nowhere else** - the horizontal offset is exactly zero - and two things follow that the dome before this fell foul of. **First, every sight line runs along the surface normal**, to within 1.43 degrees at radius 16, so the inverted Fresnel that wall was built on varied by `5.3e-10` of alpha across the entire visible sphere: a dither of 1/510 was three million times larger, and the only spatial structure on the wall was the noise. It was a flat 7% that moved a daylight sky by 15 levels and blackstone by **2 of 255**, and four green tests certified it, because every one of them measured the mean alpha over the projected disc - a property of a viewer standing *outside*, which is a viewer who cannot exist, because a domain discards itself when its owner leaves it. **Second, every meridian plane contains that eye**, and a planar curve seen from a point in its own plane projects to an exactly straight line - not nearly, exactly, at every radius and bearing. So a member drawn up a meridian is drawn as a ruler, and a straight near-vertical bar at twenty blocks is a tree trunk, a fence post or a portal jamb.

So visibility is not carried by shading at all, and the structure is not a graticule. The pane is `A_PANE` = **3%**, half what it was, a tint that carries no reading; everything legible is a **chamfer** - a lip darkening toward `BODY` meeting a lip lightening toward `GLAZE` at a one-pixel arris. The two move a background in opposite directions, so whichever lip the background defeats the other one carries, and the worst case over every background in the game has a closed form, `A_SHADOW*A_HIGHLIGHT*(GLAZE-BODY)/(A_SHADOW+A_HIGHLIGHT)` = **57 levels of 255**, which is `SubspaceOptics.chamferFloor` and is pinned by `SubspaceVaultTest.theWallCannotBeInvisible` - the test the old wall got 2 on. The arris is measured in the same `fwidth` the stroke is, so at grazing incidence both blow up together and the chamfer can never flatten into one mid-blue line. The Fresnel is kept on exactly one branch, the outside one, where `b/R` genuinely sweeps the disc and the limb genuinely is where a silhouette lives; the caster branch is fenced between two named comments and `SubspaceShaderAssetsTest.theInsideMembraneHasNoViewAngle` fails if `dot(`, `viewDir` or `shellNormal` ever appears between them.

The object: a **springing course** right round the wall, twelve **ribs** climbing from it to an open **oculus** at latitude 78 where the pane is cut away entirely, and a **plinth** tracing where the wall meets the floor. **The ribs are wound**, `bearing = 30k + twist*(latitude - springing)`, and `twist = TWIST_BASE + TWIST_PER_LAW * lawCount` - so a rib leans 23 degrees off vertical with nothing written and 52 with all twelve, and **how much law is in force is legible from the silhouette** before a glyph is parsed. That single constant answers three things at once: it takes the ribs off their meridians so they are not bars, it destroys the lat/long generator so the wall is not a wireframe globe, and it makes the shape a function of the contents. Slot *k* is `SpaceRuleCategory.values()[k]` and owns bearing 30k for good, gravity due north, so **twelve ribs wrap the whole horizon** and `floor(102.4/30) = 3` of them are in frame from every bearing there is - the reading the old band failed hardest, with all twelve marks on one arc round north and under 0.3% of the frame from three quarters of all directions. A written law makes its rib heavier (`RIB_LIT_GAIN`), colours both lips with `HudPalette.change`, lights a **notch** on the course and hangs a **boss** carrying its `SpaceRuleChange` glyph; an unwritten one is held back to `NOTCH_DIM` instead of left out, so the count has a denominator. Gravity reads twice: level is one course, inverted winds the domain the other way and lights every member from beneath, dissolved breaks the course and the plinth into dashes. Nothing has a clock - `GameTime` is forbidden in all three shader files and a test enforces it - and the only motion is three CPU one-shots: a rib climbing once as its law is written, a ring running out from a crossing, the wall fading up over 12 ticks. The springing course is placed at a constant **apparent elevation** (14 degrees) rather than a latitude, because a fixed latitude sinks underground as the radius grows; the aiming line and everything within 4 degrees of it is clear pane, which `SubspaceVaultTest.theAimingLineIsClear` holds at every radius the skill can raise.

Captures: a domain ends when its caster leaves it, so **the outside case cannot be photographed in single player** and lives in the optics test instead. Laws written at `everything` physically shove the caster off the centre of their own dome and every frame is then shot off-axis - use `everything_except_user`. The target names are `everything_except_user`, `everything`, `living_entities`, `projectiles`, `players`; there is no `mobs`. The dev world persists between runs so the stage is absolute, and `forceload add` plus twenty ticks is needed before any `fill`, or the commands run on unloaded chunks. Noon, radius 16, all twelve written, four camera angles: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoExit -PautoScreenshot=142,252,264,284,294,314 -PautoCommands="gamerule sendCommandFeedback false;gamerule doDaylightCycle false;kill @e[type=!player];magical class unlock mystic;magical unlockall;magical authority set authority_of_space;weather clear;time set noon;forceload add 68 68 132 132;tp @s 100 200 100 180 0;76:fill 68 118 68 132 118 132 smooth_stone;78:fill 84 118 84 99 118 116 sand;79:fill 101 118 84 116 118 116 polished_blackstone;90:tp @s 100 119 100 180 0;120:magical subspace create 16 true;160:magical subspace rule gravity increase_gravity everything_except_user;162:magical subspace rule velocity stop everything_except_user;250:tp @s 100 119 100 180 0;262:tp @s 100 119 100 180 -25;282:tp @s 100 119 100 180 -62;292:tp @s 100 119 100 180 28;300:time set midnight;312:tp @s 100 119 100 180 -25"`. Pitch -62 is the one that shows all twelve ribs and the oculus at once. Design: `docs/superpowers/specs/2026-09-18-subspace-boundary-design.md`.

### Chaos authority: the Pile and the Fracture

Layer -6 with the other authorities, and the one that owns no region, no entity and no grammar. **Chaos owns a Pile in the world and a Fracture in the hand.** The Pile (`magic/chaos/Pile`) is a sparse map of *sites* to one integer of **stress**; a site is a block position or a living body (`PileSite`), its **capacity** is decided by what it is and never by who owns it (`ChaosCapacity`: stone-like 4, dirt/sand/wood 2 - named by `BlockTags` rather than measured, because cobblestone and planks are both hardness 2.0 and stone is 1.5 - glass and leaves 1, unbreakable 8, a body 2 + armour/5, a player 3, **another Authority's construct 1**, which is the counter ring expressed as a physical property). A site holding more than its capacity **gives way** into its neighbours, and a neighbour that was already full gives way in turn. The wielder is a site like anything else: there is no except-the-caster clause, because the Pile is matter and matter does not know who made it.

**There is no random number in the whole Authority.** Self-organized criticality is the canonical model of a system that is completely deterministic and completely unpredictable, which is the actual definition of chaos, so the unpredictability is a property of the arithmetic. It is also why the core is pure: `Pile`, `Fracture`, `Fault`, `PileSite` and the `PileWorld` interface have no Minecraft in them at all and `PileTest` pins every rule on an exact value. `LevelPileWorld` is the only class that knows about a level, and it answers in a **stable order** - `Direction.values()` for blocks, entity id for bodies - because ties in a sandpile are broken by neighbour order and a cascade that reshuffled its neighbours would stop being deterministic.

The **Fracture** (`Fracture`, on `PlayerMagicState`, saved as a `ListTag` of five `Fault` names) is the customizable toolkit: five faults in an order, generation *n* of a cascade giving way by fault *n* and anything deeper keeping the last. So a collapse **changes character as it grows**, along a curve its owner authored - *SLUMP → SLUMP → HEAP → SHED* is a landslide that gathers, converges and detonates. The seven faults are SLUMP (all of it to the lowest neighbour), HEAP (to whichever already holds most), BLOOM (split evenly), HUNT (only to living neighbours), RECOIL (back the way it came), SHED (passes nothing on, spends it as force and harm where it stands) and ROOT (keeps it and raises its own capacity, swallowing the cascade). Seven across five positions is 16807 machines, carried on the wielder, available the day the Authority is taken.

**Entropy is the one law that knows which way time runs, so there is no CLEAR and no RESTORE**: nothing removes stress from a site. It leaves by exactly two routes - the site gives way, or the site is forgotten. The same rule settles preparation-as-a-weapon: a site that has given way goes **slack** for `Pile.SLACK_TICKS` (600) and refuses further stress, so ground you have already avalanched is dead ground and an hour of seeding cannot be re-used on the next opponent. A slack site may still *receive*, which is how RECOIL sends a cascade back into the ground it came out of without looping forever.

Four skills, three of which carry no numbers at all (`MagicCastContentKept`, `ChaosAuthorityService`): **Burden** (2 mana, 4 tick cooldown, one unit onto whatever the crosshair is on, sneak for yourself, and it is meant to be pressed dozens of times), **The Fracture** (a hold overlay, 100 tick cooldown so it cannot be re-cut inside a cascade), **The Last Grain** (4 mana, one more unit from nowhere - on ground below capacity it is a click and nothing else, and that is the skill), and **Criticality** (72 mana, 1200 tick cooldown, every capacity in the Pile drops by one for 200 ticks so everything standing lets go at once; on virgin ground it is an expensive shimmer). Their magnitude cannot be balanced by numbers because their magnitude is the work behind them.

`PileService` holds one Pile per wielder, keyed by UUID, and **it is never saved**: stress is dropped on logout and rebuilt from nothing on a change of dimension. That is thermodynamically apt and it settles griefing, the save format and unbounded growth in one decision - the kit the player authored survives, the battlefield does not. It runs the avalanche on `ServerTickEvent.Post` with a per-tick budget (`Pile.DEFAULT_BUDGET` = 48) so a long cascade visibly rolls over a second or two instead of resolving inside one frame, paints stress as `ParticleTypes.WITCH` motes every 10 ticks (**visible to everyone**, which is the whole of the counterplay: break the block it sits on, hit the body carrying it, deny the ground) and puts an `n/cap` readout on the actionbar for the aimed site. A proper crosshair pip belongs on a HUD layer through `HudBatch` and would move `HudSnapshotBudgetTest`; deferred.

Client: `client/FractureOverlay` is **five gates in a row read left to right**, deliberately not another ring of dials - Manipulate Space is three columns and a Fracture is a sequence, so it is drawn as one. Scroll turns the focused gate, left and right mouse walk it, release commits the whole sequence at once through `ApplyFracturePayload` (five ints; every ordinal is re-checked against `Fault.values()` server-side). Captures: `/magical pile load <radius> <amount>` seeds a field through the real `Pile.add` (debug only - the skill is the only real way to build one) and `/magical pile clear` forgets it. Seed a stone platform at exactly capacity so the Last Grain is the trigger rather than the seed: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoHold=cast_slot_2 -PautoCommands="gamerule sendCommandFeedback false;gamerule doMobSpawning false;kill @e[type=!player];magical class unlock mystic;magical unlockall;magical authority set authority_of_chaos;time set day;tp @s ~ ~ ~ 0 0;magical hud equip 1 fracture;125:fill ~-6 ~-1 ~-6 ~6 ~-1 ~6 stone;126:summon iron_golem ~2 ~ ~3 {NoAI:1b};127:magical hud equip 1 last_grain;129:tp @s ~ ~ ~ 0 65;133:magical pile load 4 4;143:magical-debug skill magical:last_grain" -PautoClick="100:hold;104:scroll-down;106:scroll-down;108:click-right;110:scroll-down;118:release" -PautoScreenshot=114,137,145,149,153,159 -PautoExit`. A screenshot within ten ticks of seeding can land before the first paint and show bare ground; that is the mote interval, not an empty Pile. Design: `docs/superpowers/specs/2026-09-18-authority-of-chaos-design.md`.

### Authority of Mana: incantations

Layer -6 with the other authorities. The kit is four skills, `incantation_1..4` (Incantation I to IV), and each is a press that recites one slot of the wielder's **Grimoire** (`magic/incantation/Grimoire`, on `PlayerMagicState`, saved with it): a *verse* is a card, an *incantation* is up to twenty of them in an order with a *breath* (1-8) that says how many are read per press, and the core in `magic/incantation/` (`Reciter`, pure, no Minecraft in it) turns the read verses into a `RecitePlan` - the bodies of one shot with the modifiers stamped on them, the mana they cost, the beat that becomes the cooldown. `IncantationService.recite` bills it through `MagicSinService.spendManaForSkill` (a negative bill is a refund), spawns it through `entity/verse/VerseBodySpawner` (the fan from the pattern, a deviation within the spread, statics a block ahead of the hand and dropped to the floor, Near Word on the caster, twins twelve degrees to either side), and keeps the deck per wielder and per dimension, never saved. One entity, `entity/verse/VerseBodyEntity` (`verse_body`): it reads its prototype, its stamped `ShotState` and its payload off a `ProjectilePlan` (server NBT through `ShotPlanCodec`), and **ends one way and releases the payload of that way** - a hit its Latch, its fuse its Fuse, its expiry its Epitaph; every end fires the explosion it carries and carries the caster if it is a Blink; a bounce is not an end. A body clears its target's hurt cooldown before it wounds (`VerseBodyEntity.wound`), so a fan that lands on one body lands whole where vanilla would take the first needle and refuse the rest as no stronger; `VerseBodyGameTests.aFanThatLandsOnOneBodyLandsWhole` pins it. An explosion's falloff is measured from the nearest point of each body's box rather than its feet, so a burst at a chest is a burst on that body and a body lands what the reading says (`aBodyLandsWhatTheReadingSays`). The renderer (`client/renderer/verse/VerseBodyRenderer`, one `VerseLooks` row per prototype look) draws it with the existing FX painters in its school's colour, a glyph per behaviour orbiting it and a filament per wake behind it; a bolt also wears a billboarded head, because its tube is two ribbons along the flight line and the caster it left sees that line end-on, where a ribbon is a point, so the head is what a first-person frame shows (and the beam painter takes a packed reveal: 0.5 is a whole beam, 1.0 one that has receded to nothing). Commands: `/magical incantation set <slot 1-4> <breath 1-8> <verses...>` writes a slot (bare paths are `magical:`), `know all|<verse>` learns, `show <slot>` reads it back, `preview <slot>` says what a press would cast and what it lands on one target; `magical-debug recite <slot>` presses it with the unlock, the cooldown and the pool taken care of. **The Grimoire** is the fifth skill (`grimoire`, self-managed, no price; `/magical grimoire` opens it too): `client/screen/grimoire/GrimoireScreen` over `GrimoireLayout` (pinned by `GrimoireLayoutTest`), drawn the way the Manipulate Space selector is - no panel, plate or frame, the world dimmed under drop-shadowed text and glyphs, and every HUD layer standing down while it is open (`client/hud/HudQuiet` marks such a screen; `HudLayers.gated` keeps the mod's layers off it and `HudQuietGate` cancels vanilla's hotbar, crosshair and bars through `RenderGuiLayerEvent.Pre`, leaving chat, titles and the debug overlay). One block, centred, sized for everything the wielder knows rather than the category shown, so a filter never moves the row: a line of tabs (one per slot, the open one lit with a mark that slides to it, a spark after a name with unsaved writing), a line of categories (All and every `VerseType` the catalogue has verses of, `VerseCatalogue.types()` - all eight now that the material and passive verses are in - each its glyph in its type's colour with its word beside it where the block is wide enough and the glyph alone where it is not), the known verses as a grid of their symbols (as many rows as the screen has room for, the rest by scrolling), three caption lines (the name of what the cursor is over with its type, mana and uses, then its `verse.magical.<path>.desc` split over two lines; the hint when nothing is), the incantation as a **row of twenty underlines** with the first `breath` of them lit because those are what one press reads, the breath with its steps at the row's left end and a word on what it is after them (dropped where the row is too narrow), Save and Clear at its right, and a reading that runs the pure `Reciter` on a copy of the row through `PreviewReciteWorld` on every change and says what the press lands on one target every body meets (`Landing`, pure and shared with the preview command: a flying body's hit, healing, end explosion and Latch payload, a static's pulses over its life, end explosion and Fuse or Epitaph payload; no crit and no falloff, so it is the most one target can take). Save sends `SetIncantationPayload` and waits for the state to come back matching before it says Saved. Every verse wears a symbol from `VerseSymbols` - a 9x9 glyph (its look, or its own for a modifier, a multicast, a control), a 3x3 badge in the corner for which of its family it is, counting marks along the top row - on the grid, on the row and on the ghost beside the cursor; `VerseSymbolsTest` holds all hundred distinct, every one in the table rather than a fallback, and the top row clear on any glyph that carries marks. Writing is drag and drop: a grid verse dragged over the row parts the written symbols at the boundary nearest the cursor (`GrimoireLayout.slotInsertionAt`, `SLOT_SLACK` round the row) and lands there with a small overshoot, a written verse dragged along the row is reordered the same way, one dragged off it falls away and the row closes over its place, a press that never travels three pixels is a click (a grid verse is written at the end) and a right-click on a slot strikes it; the wheel scrolls the grid and, over the row, turns the breath. Motion says only what just happened - the grid deals in on a category change, a hovered symbol grows, Save runs one bright sweep along the underlines - and `reducedMotion` cuts each to its final frame; the curves are `GrimoireLayout.ease` (overshoots), `easeOut` and `approach` (a half-life). The words row (`GrimoireLayout.wordsRow`) keeps measured widths where they fit, gives up its gap before its words and only then clamps them to equal shares; the tabs fall back to their numerals (`tabsFitWithWords`) and the categories to their glyphs (`categoriesFitWithWords`) rather than be clipped. `-PautoClick` presses and releases in place, so it clicks but never drags, and the drop arithmetic lives in the layout where the test reaches it; the window takes focus for each click, so a hover frame can show the user's own mouse if it moves during the capture. Capture, opened on a written slot once the chat has faded, the grid filtered to materials as it deals in, a spray clicked onto the row, then filtered to passives and a Halo written after it, which moves nothing in the reading (the `-PautoClick` points are GUI coordinates at scale 2, where the block sits at 40,48 with five grid rows): `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;magical class unlock mystic;magical unlockall;magical authority set authority_of_mana;magical incantation know all;magical incantation set 1 1 trident needle needle needle;time set day;251:magical grimoire" -PautoClick="268:380,76;281:56,103;303:557,76;311:100,103" -PautoScreenshot=262,270,284,296,305,313,327 -PautoExit`. Capture, a trident of needles then an orb whose fuse releases a rime ring (the golem thirty blocks out, or the needles land before a frame and the orb latches before its fuse; no screenshot within three ticks of a press, which reaches the client two or three ticks after its command tick): `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;gamerule doMobSpawning false;kill @e[type=!player];magical reset;magical hud race human;magical class unlock mystic;magical unlockall;magical authority set authority_of_mana;magical incantation know all;magical incantation set 1 1 trident needle needle needle;magical incantation set 2 1 orb_fuse rime_ring;time set midnight;tp @s ~ ~ ~ 0 0;summon iron_golem ~ ~ ~30 {NoAI:1b};121:magical-debug recite 1;181:magical-debug recite 2" -PautoScreenshot=126,134,186,193,215 -PautoExit`. Designs: `docs/superpowers/specs/2026-09-19-authority-of-mana-incantation-design.md`, `docs/superpowers/specs/2026-09-21-material-and-passive-verses-design.md` (the materials and the passives); plans: `docs/superpowers/plans/2026-09-19-incantation-core.md` (the core), `docs/superpowers/plans/2026-09-19-incantation-runtime.md` (the body and the Authority).

**Material and Passive verses.** Ten materials and five passives, the last two `VerseType`s with verses, both registered last in `VerseContent.CATALOGUE`. A material verse names a material body: `VersePrototype` carries a `Matter` (`magic/incantation/Matter`: WATER, LAVA, FLAME, STONE, GLASS, ICE, EARTH, each with its school and its lifetime) and a `MatterShape` (SPRAY and MOUND fly, FLOOD and TOUCH stand for `MATTER_STAND_TICKS`), never one without the other (`MatterTest`). The block work is `entity/verse/VerseMatter`, one method per shape: a spray lays one block on the floor under its line every `VerseBodyEntity.SPRAY_INTERVAL` ticks (`surfaceBelow` walks `FLOOR_SEARCH` blocks down for the first air over a sturdy face, so a spray over water lays nothing); a sea floods the air over the floor within its radius with real sources, which then flow as sources do; a touch converts what is already there within its radius, nearest first, up to `TOUCH_CAP` - never air, never the unbreakable, never a block entity, never what it already is - so a Touch of Stone seals a golem to the knees and turns a sea to stone; a clod mounds five blocks and one on top where it lands, into air and never into a body. Every block goes through `ConjuredTerrainService` and into `entity/verse/MatterKeeper` (static per dimension, never saved) with the matter's lifetime, and comes back. Two ledger rules came with them: `replace` over a position another edit already holds hands the original over (`Ledger.release`), so a touch over a sea gives the ground back once and never leaves the water (`aTouchOverASeaGivesTheGroundBackOnce`); and an edit the level loads with is an orphan, adopted at load and given back by `restoreLoadedOrphans` from the `ConjuredTerrainService.tick` sweep every `ORPHAN_SWEEP_TICKS` once its chunk is here, through `level.setBlock` so the neighbours hear of it - a write straight into a loading chunk tells nobody and leaves the water that flowed from a conjured source standing for good (`ConjuredTerrainGameTests`). A passive is transparent when read (`PassiveVersesTest.aPassiveIsTransparentToComposition`: the plan with it is the plan without it) and acts every tick while written in any slot: `magic/incantation/VersePassives` is a `ClassPassiveHandler` reading `PassiveVerses.written(grimoire, catalogue)` (each once, in the order first met) - Taper keeps a `Blocks.LIGHT` at the eyes through a conjured edit and Storm Taper shocks round it, Halo and Half Halo turn a projectile closing on the wielder (`Projectile.deflect`, the arrow theirs after; the full ring for `HALO_COST`, the forward `HALF_HALO_DEGREES` for `HALF_HALO_COST`) with a clash flare at `FLARE_SCALE` (`SkillClashEffectEntity` grew a synced scale for it) and one shield sound, and Familiar throws a needle at the nearest hostile in sight every `FAMILIAR_INTERVAL` for `FAMILIAR_COST`. A fake player never gets a player tick, so `VersePassiveGameTests` drives `ClassPassiveEffects.tick` by hand. Every verse wears a `VerseSymbols` glyph (SPRAY, SEA, HAND, CLOD, CANDLE, HALO, HALF_HALO, GHOST; badges MOLTEN, BRICK, PANE). Captures build the stage rather than trust the beach - the dev world keeps every earlier run, `forceload add` plus twenty ticks must precede a `fill`, `gamerule commandModificationBlockLimit` lifts the 32768-block cap so one `fill` clears the air above, and a `fill` that changes nothing is a failure that prints over the gamerule, so the floor is laid as sandstone first and then as what it is. `chatVisibility:2` in `run/options.txt` is not the way to an empty chat: with the chat hidden the auto-commands are never sent at all. Materials at noon from a two-block pillar (a sea at the feet floods the caster, a touch from the ground entombs them): a clod aimed at the sand and its mound, a spray fired level and then seen broadside from a second pillar (end-on, the fires hide behind the nearest one), a sea round the golem, a touch over it: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;gamerule doMobSpawning false;gamerule doDaylightCycle false;gamerule commandModificationBlockLimit 1000000;kill @e[type=!player];magical reset;magical hud race human;magical class unlock mystic;magical unlockall;magical authority set authority_of_mana;magical incantation know all;magical incantation set 1 1 clod;magical incantation set 2 1 spray_flame;magical incantation set 3 1 sea_water;magical incantation set 4 1 touch_stone;time set noon;weather clear;forceload add 70 70 130 130;tp @s 100 200 100 0 0;76:fill 70 117 70 130 118 130 sandstone;78:fill 70 117 70 130 117 130 stone;80:fill 70 118 70 130 118 130 sand;82:fill 100 119 98 100 121 98 smooth_stone;84:fill 70 119 70 130 140 130 air;86:fill 100 119 98 100 120 98 smooth_stone;87:fill 108 119 90 108 121 90 smooth_stone;90:tp @s 100.5 121 98.5 140 25;92:summon iron_golem 100.5 119 101.5 {NoAI:1b};351:magical-debug recite 1;425:tp @s 100.5 121 98.5 180 0;431:magical-debug recite 2;445:tp @s 108.5 122 90.5 90 30;505:tp @s 100.5 121 98.5 0 25;511:magical-debug recite 3;591:magical-debug recite 4" -PautoScreenshot=357,367,381,437,441,451,461,517,531,597,611 -PautoExit`. Passives at midnight with `gamma:0.0` in `run/options.txt` (at the dev gamma the taper's light is invisible on a beach), a golem ahead because a familiar wants a hostile and a husk is dead by the seventh needle: a taper written and the sand lit round the wielder, a halo written and an arrow summoned flying in, turned at the ring with the flare, a familiar written and its needle on its way: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;gamerule doMobSpawning false;gamerule doMobLoot false;gamerule doDaylightCycle false;gamerule commandModificationBlockLimit 1000000;kill @e[type=!player];magical reset;magical hud race human;magical class unlock mystic;magical unlockall;magical authority set authority_of_mana;magical incantation know all;time set midnight;weather clear;forceload add 70 70 130 130;tp @s 100 200 100 0 0;76:fill 70 117 70 130 118 130 sandstone;78:fill 70 117 70 130 117 130 stone;80:fill 70 118 70 130 118 130 sand;82:fill 100 119 98 100 121 98 smooth_stone;84:fill 70 119 70 130 140 130 air;90:tp @s 100.5 119 100.5 0 0;92:summon iron_golem 100.5 119 107.5 {NoAI:1b};351:magical incantation set 1 1 taper;561:magical incantation set 2 1 halo;771:summon arrow 102 120.6 104.5 {Motion:[-0.4,0.0,-1.0],NoGravity:1b};791:magical incantation set 3 1 familiar" -PautoScreenshot=345,557,772,774,777,997,1001,1005,1009,1013,1017,1021,1025 -PautoExit`.

### Sin system

`magic/MagicSinService.java` — 7 deadly sins track player behavior (Pride from full-barrier casts, Wrath from taking damage, Greed from hoarding, Sloth from idling, etc.). Sin passives boost stats when gauge is high. Sin curses impose penalties if sin gauge reaches max. Accumulated in `PlayerMagicState`.

### Forged strikes (the blacksmith/divinesmith left click)

An attack press holding a `magical:forged_weapon` runs `ForgeComboService`/`ForgePayloadService` -> `ForgeStrikeEntity.spawn` -> synced data -> `client/renderer/ForgeStrikeRenderer.extractRenderState` -> `ForgeForms.render` -> one of eight geometry classes in `client/renderer/forge/` (seven share one `forgeEdge()` consumer; SLAM spans two render types and is handed the source, because `ForgeBuffers` forbids two live consumers on the shared buffer).

Seven of the eight are built from `ForgeRibbon`. An arc of blade is a **closed solid**, not a sheet: four faces per segment, a lens cross-section (`crossSection`) that is sharp at both lips and thickest on the spine, tapered along its length by `bladeProfile`, with `planar(plane, u, v, n)` putting the thickness on the one axis the plane does not span.

**A swing may lean; a thrown thing may not.** An arc has a belly and a belly points somewhere — fine for a swing, which is a thing done in a direction with the wielder standing at the middle of it, and wrong for the wave, where whichever direction gets picked is arbitrary *and* is not the direction the wave is travelling. The wave was drawn tipped up-right, then bowed belly-down, and both were the same mistake. So it is not an arc at all: `ForgeWaveFront` is a **surface of revolution about the aim** — `point(radius, depth, angle, across)` gives the angle no say in depth, so the middle leads, the rim trails, and the sheet is identical at every roll angle about the flight line. `WaveSilhouetteTest` pins exactly that, plus the sizing below. Rings bunch toward the rim (`RIM_BUNCH`) so the shader's cutting lip lands on a thin band and the front ends on an edge; `halo` is its rim glow, written there rather than borrowed from `sheath` because a sheath tapers to nothing at both ends of its arc and a ring has no ends — a seam would be an in-plane direction again. Its wake is short and tapers hard (`TRAIL_GAP`, `TRAIL_SHRINK`): the thrower stands *behind* a wave, so every trail copy is nearer the camera than the head and perspective alone draws it larger.

**A drawn form must sit on the volume that actually hits.** An arc is measured from the centre of its own circle, so a crescent built at radius R puts every one of its pixels R blocks from the strike and none of them on it — which is how the wave came to be a band hanging between 1.5 and 3 blocks off the flight path, rolled 40 degrees, claiming nearly four times the width it catches in. `WaveSilhouetteTest` measures the drawn sheet against `HitShapes.forFamily(WAVE).broadInflation(halfWidth, reach)` — `WaveShape` inflates the swept box by `halfWidth * 0.48` laterally and a flat `0.9` vertically, and its `hits` only rejects things behind the wave, so that box *is* the hit volume. The vertical `0.9` does not grow with the weapon, so `WaveGeometry.radius` is a `min`, not a fraction: a heavy divine blade would otherwise be drawn reaching half again as far up and down as it catches. The glow and the wake are deliberately outside the measurement: a glow is light, and a wake is where the blade has already been, which the swept box covers. `ForgeMotion` opens an arc along its own span (`opening`) or grows a thrown one out to its reach (`reaching`, and `reached` for the factor alone, so the wave's front and the ring of ornaments round its rim cannot drift apart) - both capped so the drawing never claims more than the hit shape. Three things then sit on top of the steel, and all three exist because of one complaint - a swing seen down the line of its own plane is close to a line, and a picture of nothing but smooth ribbons reads as fog:
- **`ForgeAura`** - the one call each form makes after its arc. It reads the viewer's position straight off the pose matrix (`ForgeView.eye`, the inverse applied to the camera-relative origin), so a form that lifts, tilts, rolls or fans its arc is accounted for without saying so.
- **`ForgeRibbon.sheath` / `lanceSheath`** - a camera-facing glow band along the blade's spine. Its v runs 0.5 at the centreline to 0 at both rims, so it is symmetric and the shader's cutting-lip term never fires on it. Drawn once per strike, not once per trail copy.
- **`ForgeSparks`** - debris off the cutting lip, hashed from the entity id so every client sees the same shower. Slim camera-facing streaks; `count` is the grade's.

`rendertype_forge_edge.fsh` is where a strike gets its colour. The render type blends **`ONE, ONE`** - the alpha channel is discarded outright - so the vertex alpha must be folded into rgb, exactly as `magic_common.glsl`'s `additiveOut` does; `CEILING` is set from the layer stack (two faces x four to six trail copies ~ 3.5x) so the hottest pixel lands near white and everything short of it keeps the element's hue. `core` is the one term with a hard boundary and is deliberately kept out of the noise.

The weapon's own numbers show: element owns colour and ornament (`ForgeElementAccent`), form owns the silhouette, temper and modifiers already move `halfWidth`/`reach` through `ForgeStrikeMath`, and **`ForgeWeaponLook`** adds the last two - grade scales emission 0.62..1.28 and spark count 3..18, quality scales blade breadth 0.86..1.18 - applied once in the renderer, off `state.alpha` and `state.halfWidth`, so all eight forms inherit them.

Captures: a strike is born from a left click, and `-PautoClick` only reaches an open screen while `-PautoHold` only reaches a registered key mapping, so neither can press attack. `forge/ForgeStrikeBench` spawns one through the real `ForgeStrikeMath.resolve` and `ForgeStrikeEntity.spawn`: `magical-debug forge strike <form> <element> [class] [grade] [heavy]` for one, `magical-debug forge bench <element> [class] [grade] [heavy]` for one of every family at once (`heavy` is a literal, not a boolean). Judge colour at **midnight** - additive over daylight sand clips to white by construction, which every glowing effect in the mod shares - and judge shape in **first person**, which is how it is played and the angle every arc is most nearly edge-on at. The wave is the exception and needs a second camera off the flight line — a dome and a flat disc are the same picture seen down their own axis, so face-on says nothing about whether it curves; throw it facing `0 0`, then `tp @s ~-4 ~1 ~-9 -20 25` a tick or two later for the three-quarter view that shows the cup. Forms live 3 to 6 ticks (a wave 20), so a screenshot lands one or two ticks after the spawn: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="gamerule sendCommandFeedback false;magical reset;magical hud race human;magical class unlock blacksmith;time set midnight;tp @s ~ ~ ~ 0 0;140:magical-debug forge strike magical:slash magical:fire sword divine;150:magical-debug forge strike magical:cleave magical:fire sword divine;160:magical-debug forge strike magical:thrust magical:frost sword divine;170:magical-debug forge strike magical:spin magical:fire sword divine;180:magical-debug forge strike magical:slam magical:fire sword divine;190:magical-debug forge strike magical:wave magical:void sword divine;215:magical-debug forge strike magical:rising magical:fire sword divine;225:magical-debug forge strike magical:flurry magical:fire sword divine" -PautoScreenshot=141,151,161,171,181,193,216,227 -PautoExit`; add `-PautoCamera=third_back` for the same run seen from outside.

### Weapon item models

Every weapon in `forge/weapon/MagicalWeapons` ships three files under its own path -
`assets/magical/items/<path>.json`, `models/item/<path>.json`, `textures/item/<path>.png` - and
`MagicalWeaponsTest.everyWeaponHasItsThreeAssetFiles` checks all three exist. There is no datagen
for them; they are written by hand.

All of them are one flat quad on `minecraft:item/handheld` wearing a 16x16 icon, **except
Duskfall**, which is a real 130-cuboid Blockbench export (source project and previews at
`D:\bb\eclipse_reaver`, authored as a standalone 1.21.4 pack). Importing one of those is easy to
get wrong in ways that never fail a build, so `WeaponModelAssetsTest` pins the three that matter
for every weapon model at once. **The namespace**: an export carries its own pack namespace on
every texture reference, and a reference into a namespace this mod does not ship resolves to the
missing texture while the model loads perfectly - so `eclipse_reaver:item/eclipse_reaver` had to
become `magical:item/duskfall`, and that rename is the one thing the test caught. **The UV space**:
`texture_size` is a Blockbench field that vanilla does not read at all, so UVs are always 0..16 no
matter how many pixels the sheet has; an export that leaves them in texture pixels samples a
two-pixel corner across the whole sword and the parser accepts it, because any float is a valid UV.
**The bounds**: vanilla refuses an element outside -16..32 or a rotation that is not 0, +/-22.5 or
+/-45 on one axis, and that refusal is a load-time log line, not a test failure, so the weapon
simply has no model in the finished game.

The display transforms need retuning on import and the test cannot help, because nothing about them
is wrong in the abstract - they are wrong relative to this mod. A standalone pack tunes its hero
item to fill the frame; here the same numbers made a sword 31 model units tall (about twice a
vanilla blade) cover a third of the screen in first person. First person went 0.72 -> **0.42**,
which puts its on-screen footprint beside a netherite sword, and the GUI went 0.46 -> **0.55** with
the translation pulled back toward centre. The GUI scale has a hard ceiling: the slot is 16 units,
the blade rotated -35 degrees runs along the slot diagonal at 22.6, so 0.62 clips the tip and the
grip out of the slot and 0.55 is the last value that does not. Third person was left exactly as
authored; it was already right.

### Visual conventions (from SKILL_CREATION_NOTES.md)

- No reused vanilla projectiles, fireballs, or particles as main identity.
- Invisible gameplay entities + custom quad/beam/ring renderers.
- Custom core shaders in `assets/magical/shaders/core/` (`.vsh`, `.fsh`, `.json`), registered via `RegisterShadersEvent` → `MagicalRenderTypes`.
- Skills: unique mechanics first, visuals second.

### HUD

`client/hud/` — the in-game overlay, "the sigil": concentric ring meters (XP, mana, barrier, the charge halo) round a core that carries the numerals (level, mana, barrier, vault), a crown of sin satellites, a fan of cast cards on spokes with key tags and a seconds numeral while cooling, text readouts for the lit sins beyond the tags, captions under the fan, a reflection of the pools under a waterline for blood/dark mages, status chips under the crosshair, and announcements of what the player just gained. The hold overlays (sub-skill wheels, space dials, blood strip), the counter box and the combo readout are the original `GuiGraphics` code, untouched. **The loadout switcher is not** - it is a frameless rail now (`client/MagicWheelOverlay`, geometry in `client/LoadoutSwitcherLayout`, pinned by `LoadoutSwitcherLayoutTest`), and it is the case that settles what frameless means here. It was a 148px `MagicalGuiStyle.panel` whose caption was drawn with no truncation at all, so thirty-four characters of "Scroll to choose, release to switch" ran out through its own right wall onto the sky. **Frameless does not mean bare text on the world: it means a scrim.** A drop shadow preserves the shape of a glyph and does nothing for its contrast, and the numbers are brutal - measured against real noon sand (220,209,165), `TEXT_PRIMARY` lands at **1.45:1** and `TEXT_MUTED` at **2.06:1**, so the highlighted row reads *fainter* than the rows it is picked out from and the highlight inverts. Every frameless surface in the mod already dims the world for this reason (`SpaceManipulationOverlay` 0xA6, `FractureOverlay` 0x76); the switcher does it at 0x8C, which takes the same sand to (71,86,89) and the three inks to 7.23 / 5.44 / 4.71:1, in the right order. Three readings share the rail and each owns an independent channel so they cannot be confused: where the wheel points is **position and brightness** (a `>` in the gutter, the only white text on screen), what is worn is **hue** (gold, with a hairline under the name), and whether a release will do anything is the **accent colour** of that mark plus one word on a line that is reserved whether or not it is used - a cast can lock the swap while the list is open, because the cast keys still fire while B is held. The pips lead the row rather than trail it, because a trailing run needs a right edge and the right edge was the frame; an empty slot is a dash rather than a dark dot, so it differs by shape and not only by tone. `LoadoutSwitcherLayout.textLimit` is a function of `guiWidth` rather than a constant, the way both other frameless layouts clamp theirs, and `nothingReachesTheCrosshair` is the old caption bug pointed the other way. Capture: `-PautoHold=open_wheel` with `-PautoClick="120:hold;150:scroll-down;190:release"`, and `magical hud swaplock <ticks>` forces the locked state, which a cast cannot do in an unattended run because you cannot press a cast key and hold the switcher key at once. Rules:
- **Layers, not a render event.** `HudLayers` registers `magical:first_person` / `sigil` / `rule_flash` / `encounter` / `selector` on `RegisterGuiLayersEvent`; the sigil layer also calls the original combo and counter renderers, the selector layer the original hold overlays. Modded layers are not gated by `hideGui` (`Gui.java:265` wraps only vanilla's groups); `HudLayers.gated` does it.
- **Tick builds, render reads.** `HudState.tick` (end of `Hud.onClientTick`, both branches) rebuilds an immutable `HudSnapshot` only when a version changed (`ClientMagicState`, `ClientCooldowns`, `ClientStatusState`, `HudAnnouncer`, keybinds, window, language, options). Renderers are pure functions of the snapshot + partial tick: no allocation, no `PlayerMagicState` calls, no registry lookups. Moving values are `HudTween`s; the only per-tick string is a cooling card's seconds, reshaped when the second changes.
- **One batch.** Everything is a quad on `MagicalFxRenderTypes.hudSigil()` (`rendertype_hud_sigil.fsh`, eleven kinds mirrored in `HudKind`) emitted through `HudBatch` inside one `drawSpecial`; text goes after it through `HudText` with vanilla's drop shadow. Never `GuiGraphics.fill` under `client/hud` — a test forbids it. `HudSnapshotBudgetTest` pins the quad count.
- **Geometry in `HudLayout`, colours in `HudPalette`, glyphs in `HudGlyphs`.** `HudLayoutTest` sweeps every anchor × scale × state for overlap and fit at 480×270, and checks the core holds its numerals. A skill's icon is its cast circle's emblem; sins and statuses are stamps.
- **Cooldowns** are not in the state blob; `CooldownSyncPayload` (dirty-set flushed from `PlayerMagicState.sync`/`tickServer`, full list on login/respawn/dimension change) feeds `ClientCooldowns`, which extrapolates. The original wheels read it too.
- **Options** in `MagicalClientConfig` (`config/magical-client.toml`): anchor, scale, opacity, sins, statuses, compact, reducedMotion, hudDebug. `/magical hud …` forces every HUD state (sins, charge, gluttony, corruption, vessel, status, cooldown, equip, race); `-PautoHold` captures a hold overlay.
- **The rule flash** (`magical:rule_flash` layer, `RuleFlash` + `RuleFlashRenderer`): when `SpaceAuthorityService.applyRule` accepts a Manipulate Space rule it sends `SpaceRuleAppliedPayload` to the caster, and the client pops the category's formula (`magic/SpaceRuleNotation`, one bracketed symbol per category) above the crosshair for 48 ticks with the symbol tinted and moved by the operation's `SpaceRuleChange` (RAISE / LOWER / ZERO / FLIP / LOCK / SURGE / AIM / RESTORE), one `RULE_MARK` quad playing the kind's mark behind it, a caption, a faint `fp_overlay` wash, and two synthesized cues (`assets/magical/sounds/rule/*.ogg`, from `scripts/synth-rule-cues.py`; `MagicalSounds.cue`). Three quads and at most five strings, only while live (`RuleFlashBudgetTest`); nothing when idle; `reducedMotion` shortens it to 32 ticks with static marks and no wash. `/magical hud rule <category> <operation> [target]` fires one without a subspace: `.\gradlew runClient -PquickPlay="New World" -PautoCommands="magical hud rule gravity increase_gravity everything" -PautoScreenshot=52`.

### Magic Codex screen

The pyramid's rows are keyed by a skill's `tier`, and a row draws one list, so **two schools may never share a tier**: the second one is unlocked, castable and invisible. Below the line one layer is one school - -1 Blood, -2 Dark, -3 Chaos, -4 Primordial, -5 Eldritch, each named by `tier.magical.below.<layer>` - and the Authority family sits alone on `MagicPyramidMenu.AUTHORITY_TIER` (-6) below all of them, drawn from `MagicContent.authoritySkills()` rather than from its tier. `MagicContent.minTier()`/`maxTier()` count only skills with a row of their own (no authority, no sub-skills). `PyramidLayersTest` pins all of it.

`client/screen/MagicPyramidScreen` (its menu, `magic/menu/MagicPyramidMenu`, carries only button ids and data slots) wears the same frame as the creator: `client/screen/ScreenChrome` owns the 444x340 panel, the top strip (title, chip, XP bar, corner button), the hairline, the tab row and the body panel, and paints them for both screens. Four tabs - Skills (the pyramid, the owned-skill emblem rows, the active loadout's four key cards, the skill detail with its tuning), Loadouts, Passives, Classes - replace the old Back-button sub-views. **A key card takes two presses** (`MagicPyramidMenu.cardPressOpensSkill`, which the menu and the screen both ask so the client cannot guess a different answer a tick early): the first moves the cursor onto the key and nothing else, the second, on the key the cursor is already on, selects the skill bound there - which flips the pyramid, scrolls the list and fills the detail. One press used to do both, so reaching for the key you were about to bind a picked skill onto threw the picked skill away. The backdrop behind the whole frame is `MagicalGuiStyle.BACKDROP`, one 64px tile repeated by handing `blit` the tile size as the texture size and the panel size as the drawn size (u and v run past 1 and the sampler wraps, exactly as vanilla lays its own menu background; a GUI texture clamps only when its mcmeta says so and this one has none), with the old fall-off kept as a gradient over it rather than baked in. `ScreenBackdropTextureTest` pins the tile to the size the code repeats it at and holds its wrap seam to the worst seam it already carries inside itself, because a tile that does not meet itself paints a grid across the panel and nothing fails. Every rectangle is in `CodexLayout`, and `CodexLayoutTest` pins per tab that nothing overlaps, everything sits in the body, and each hit-test answers to its centre and to nothing in the gaps. Emblems come from `client/screen/EmblemPainter` (shared with the creator) in one `drawSpecial` before the tooltips. `-PautoClick="tick:x,y;..."` moves the pointer onto each point and presses the open screen for captures - it asks for window focus first, because GLFW drops a cursor move on an unfocused window without a word and an unattended capture runs unfocused, so a hover state or a tooltip can be captured too - and the codex is `HudDebug.Captured`, so one launch walks the tabs: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="magical unlockall;101:magical codex" -PautoClick="118:247,45;132:339,45;146:431,45" -PautoScreenshot=112,126,140,154 -PautoExit`.

### Spell Creator screen

`client/screen/creator/` - the workshop where the Spell Creator / Magic Originator fuses two owned skills into a third. A plain `Screen` with no container menu behind it (no inventory, no button ids): `SpellCreatorLayout` is pure geometry pinned by `SpellCreatorLayoutTest` (nothing overlaps, every hit-test is a function of the same numbers); `SpellCreatorScreen` paints chrome, then every skill emblem in one `drawSpecial` through `EmblemPainter` (the HUD sigil atlas: `HudGlyphs.skillCell` + `HudPalette.cardTint`), then text; `FormulaListPanel` is the Formulas tab. Status comes from `MagicFusionService.status` / `formulas` (`READY`, `MISSING` inputs, `LOCKED` by class, `CREATED`; sorted in that order, stable by recipe). Opened by `OpenSpellCreatorPayload` (sent by `ClassTreeMenu` and `/magical creator [formulas | create <first> <second>]`) or straight from the codex Classes tab; the one thing it sends is `CreateSkillPayload` -> `MagicFusionService.create` (`/magical create <first> <second>` is the same call, for captures). Back sends the codex request. `/magical reset` wipes the magic state for a fresh start. `isPauseScreen` is false so the integrated server answers Create; it implements `HudDebug.Captured` so the auto-closer leaves it up: `.\gradlew runClient -PquickPlay="New World" -PwindowSize=1280x720 -PautoCommands="magical reset;magical class unlock spell_creator;magical unlockall;101:magical creator create wildfire abyssal_discharge;121:magical create wildfire abyssal_discharge;141:magical creator formulas" -PautoScreenshot=112,128,152 -PautoExit`.

### Custom dimensions

Three datapack dimensions under `data/magical/dimension{,_type}/`:
- `chronos_end` — the Chronos (Authority of Time) dimension
- `dungeon_tower` — the Dungeon Tower dimension (with `tower/DungeonTowerService.java`)
- `pocket_space` — personal pocket dimension (managed by `SpacePocketService`)

### Adding a new skill

1. `MagicContent` — add `register(...)` line; add id to relevant classifier set (`SUB_SKILLS`, `AUTHORITY_SKILLS`, `CLASS_REWARD_SKILLS`).
2. `MagicCastingService.castResolved` — add behavior (id branch in `if`-ladder, or generic type-based cast).
3. New entity → `entity/` class + `registry/MagicalEntities` + renderer in `client/renderer/` + bind in `MagicalClientEvents.registerRenderers`.
4. Hold/charge/multi-mode → payload in `network/` + handler in `MagicalNetwork` + client input handler wired in `MagicalClientEvents.Hud.onClientTick`.
5. Lang keys in `assets/magical/lang/en_us.json` (`skill.magical.<path>` + `.desc`).
6. Class grant lists in `MagicalClasses` if class-rewarded. Passives in `MagicPassiveContent` if passive-gated.
7. The HUD needs nothing: its card icon is the cast circle's emblem from the visual profile, and the cooldown sweep and seconds are automatic. Only a skill with no emblem needs a stamp in `client/hud/HudGlyphs`.

Arcane content goes in `arcane/ArcaneContent` (rune/shape/modifier records) — separate flow, not the `magic/` system.